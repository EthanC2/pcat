#! /bin/bash

# 1. Create the ~/.pcat folder
# 2. Create dependents: settings.ron, .pcat/templates folder
# 3. Compile the source code file and add it to the system with the name 'pcat'
# 4. Prompt the user if they want to install the default main program templates (cpp, py, java, ...)